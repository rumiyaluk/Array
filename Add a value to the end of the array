Add a value to the end of the array
Write a function addValue that takes an array arr and value as arguments, adds value to the end of the array, and returns the resulting array.
Example:
• the addValue ([1, 2, 3], 10) function must return [1, 2, 3, 10];
• the addValue ([1, 1, 1], "a") function must return [1, 1, 1, "a"];
• the addValue ([], 10) function must return [10].

Solution
function addValue (arr, value) {
  let res = arr.push (value);
  for (let i = 0; i <arr.length; i ++) {
  }
  return arr;
}
